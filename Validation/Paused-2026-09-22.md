# Interrupted safety run, 2026-09-22

Historical evidence from the run interrupted before profile application. Work subsequently resumed; see the current safety matrix in the repository README.

- Device set: `/private/tmp/SwiftSimSlim-safety-20260922-c/Devices`
- UUID: `FD8EC1CA-FDF4-46D5-AD4D-219DF550564A`
- Device: SwiftSimSlim Safety QA, iPhone 18 Pro, iOS 27.0.
- Cold boot/readiness completed in 118.969 seconds.
- AcceptanceFixture installation was still pending at interruption; only a placeholder had been observed. No Slim profile was applied and no before/after measurements were obtained.
- Test runner exited with status 65. The isolated simulator was explicitly shut down, and a fresh device listing confirmed Shutdown.
- Operation and runner logs are preserved as `2026-09-22-paused-operations.log` and `2026-09-22-paused-run.log`.
- The interrupted `.xcresult` at `/private/tmp/SwiftSimSlim-safety-20260922-c.xcresult` is not success evidence.

The earlier failed setup is separately preserved in `2026-09-22-safety-matrix.json`.

## Follow-up stopped after two further runs

The next run confirmed managed-state persistence but exposed a fixture crash before Slim: UIKit reported `UIApplicationEvaluateRuntimeIssueForNoSceneLifecycleAdoption`. The fixture was updated to scene lifecycle, and the harness now requires a per-launch readiness marker and a unique pre-Slim document. The subsequent run installed the fixture but timed out during launch before Slim. Both new devices were deleted by the test. See `2026-09-22-fixture-crash-matrix.json` and `2026-09-22-launch-timeout-matrix.json`.

Further live tests are paused due to host load. No iOS 18.6 test was started. Resume only when requested, using a fresh unique test root. Successful end-to-end saved restoration and valid before/after memory results remain outstanding. The earlier interrupted device `FD8EC1CA-FDF4-46D5-AD4D-219DF550564A` remains shut down in its isolated set for later cleanup; it was not rebooted during this continuation.

## Verification resumed, then stopped on UI confusion

The next isolated live run (`SwiftSimSlim-safety-20260922-f`, UUID `B80FED37-27E9-4725-BC70-F93FE3078C54`) again timed out on fixture launch before Slim. The test deleted the device and verified the set empty. Evidence: `2026-09-22-repeat-launch-timeout-matrix.json`. No successful live restoration or valid memory result is claimed.

A macOS UI test with a synthetic backend successfully opened Export Saved State, cancelled the save panel, and reopened it. Automation then timed out while typing the export filename, before saving or restoring. Those remaining UI steps are not verified. UI and live runs were stopped at the user's request.

A confusing test-host behavior was identified: ordinary tests used an empty backend while still displaying the normal simulator window. Unit test hosts now expose only a Settings scene; UI test modes display an explicit banner, and fixture startup errors are surfaced instead of silently showing an empty list. The window-persistence UI test uses its own explicitly marked empty UI mode. Further test execution is paused; normal app startup is restored to the SwiftSimSlim scheme / My Mac.

A read-only default-device listing confirmed five user devices still present: iPhone 18 Pro, 17 Pro 1, 17 Pro 2, 17 Pro 3, and iPhone Duo. Only 17 Pro 1 and 17 Pro 2 were booted; no user devices were changed by this verification.

The final test-host separation compiled successfully with `SwiftSimSlim` build-for-testing (2026-09-22 16:12 local). No tests were rerun after the stop request, so the new entry-point routing is build-verified only. A normal existing Release app was opened without test variables. Export cancellation/reopening is the only successful interactive evidence from the interrupted UI run; file saving and Restore remain pending.
